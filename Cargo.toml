[package]
name = "md-reader"
version = "0.0.1"
edition = "2024"
license = "MIT"

[dependencies]
katex-rs = "0.2.4"
pulldown-cmark = "0.13"

[profile.release]
lto = "thin"
codegen-units = 1
strip = true
